@using RestaurantProject.WebUILayer.DTOs.ServiceDTOs
@model List<ResultServiceDTO>

@{
    ViewData["Title"] = "Hizmet Listesi";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    int id = 0;
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h4 class="mb-0">Hizmet Tablosu</h4>
                <a href="/Admin/Service/CreateService" class="btn btn-success">Hizmet Oluştur</a>
            </div>

            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <tr>
                            <th class="text-center">#</th>
                            <th>Başlık</th>
                            <th>Açıklama</th>
                            <th>İşlem</th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            id++;
                            <tr>
                                <td class="p-0 text-center">@id</td>
                                <td>@item.ServiceTitle</td>
                                <td>@item.ServiceDescription</td>
                                <td>
                                    <a href="/Admin/Service/DeleteService/@item.Id" class="btn btn-danger">Sil</a>
                                    <a href="/Admin/Service/UpdateService/@item.Id" class="btn btn-primary">Güncelle</a>
                                </td>
                            </tr>
                        }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
