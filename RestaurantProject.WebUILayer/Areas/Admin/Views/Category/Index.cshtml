@using RestaurantProject.WebUILayer.DTOs.CategoryDTOs
@model List<ResultCategoryDTO>

@{
    ViewData["Title"] = "Kategori Listesi";
    Layout = "~/Areas/Admin/Views/Shared/_Layout.cshtml";
    int id = 0;
}

<div class="row">
    <div class="col-12">
        <div class="card">
            <div class="card-header d-flex justify-content-between align-items-center">
                <h4 class="mb-0">Kategori Tablosu</h4>
                <a href="/Admin/Category/CreateCategory" class="btn btn-success">Kategori Oluştur</a>
            </div>

            <div class="card-body p-0">
                <div class="table-responsive">
                    <table class="table table-striped">
                        <tr>
                            <th class="text-center">#</th>
                            <th>Kategori Adı</th>
                            <th>İşlem</th>
                        </tr>

                        @foreach (var item in Model)
                        {
                            id++;
                            <tr>
                                <td class="p-0 text-center">@id</td>
                                <td>@item.CategoryName</td>
                                <td>
                                    <a href="/Admin/Category/DeleteCategory/@item.Id" class="btn btn-danger">Sil</a>
                                    <a href="/Admin/Category/UpdateCategory/@item.Id" class="btn btn-primary">Güncelle</a>
                                </td>
                            </tr>
                        }
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>
